#Now, while on the new branch (not main) create a new folder in your local RRDS26-
 # Workshop2 directory called Activities and copy these Activity 2 Instructions into that folder.
#Then add and commit that change (don’t push or publish it).